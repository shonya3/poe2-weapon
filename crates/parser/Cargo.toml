[package]
name = "parser"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1.0.217", features = ["derive"] }
serde_json = "1.0.136"
weapon = { path = "../weapon" }
